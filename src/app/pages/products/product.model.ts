export class Product {

    constructor(public product_id: number, public name: string, public description: string, public brand: string, public price: number, public category: string, public imageURL: string) {

    }
}