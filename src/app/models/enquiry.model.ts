export class Enquiry {

    constructor(public enquiry_id: number, public full_name : string, public email: string, public category: string, public message: string, public status: string, public response: string) {

    }
}